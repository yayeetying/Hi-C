-If we look at the JSON file's raw data, we can see that each key-value pay is separated by commas - we can convert this into a dictionary using .loads()
-Get request

-let's say j is a dictionary containing JSON info; if you return j, you serve information as JSON info (pink text); if you return string(j), you serve the dictionary's information as a string
